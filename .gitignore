# Python
__pycache__/
*.pyc
.venv/
venv/

# Local environment
.env

# Personal data — never commit (this is a public repo)
data/portfolio.json
data/profile.md
data/notebook.md
data/predictions.jsonl
data/*.csv
data/watchdog_state.json
data/*.log

# Autopilot runtime state (local paper trading — never commit)
data/autopilot/

# Bank statements — highly sensitive, never commit (public repo)
bank-statements/

# Personal tax briefing — keep local
data/contador-briefing.md
.vercel

# macOS
.DS_Store
